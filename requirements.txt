# Requirements for Streamlit Cloud deployment
# Streamlit Cloud reads this file to install dependencies.
# For local development, use: pip install ".[dev]" (reads pyproject.toml)

streamlit>=1.40.0
torch>=2.0.0
transformers>=4.30.0
pyyaml>=6.0
